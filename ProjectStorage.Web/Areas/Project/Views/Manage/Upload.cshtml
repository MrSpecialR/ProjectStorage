@model ProjectStorage.Web.Areas.Project.Models.ProjectCreateModel

@{
    ViewBag.Title = "Upload an Project";
}

<br />

<h2>Upload a Project</h2>
<hr />
<form method="post" enctype="multipart/form-data">
    <div class="col-md-6">

        <div class="form-group row">
            <label asp-for="Title" class="col-2 col-form-label">Title</label>
            <div class="col-10">
                <input asp-for="Title" class="form-control" />
            </div>
        </div>
        <div class="form-group row">
            <label asp-for="File" class="col-2 col-form-label">File</label>
            <div class="col-10">
                <input type="file" id="File" name="File" class="form-control" accept=".zip" />
            </div>
        </div>
        <input type="submit" class="btn btn-primary" value="Create" />
    </div>
</form>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}